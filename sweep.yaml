program: config.yaml
method: bayes
metric:
  name: validation_loss
  goal: minimize
parameters:
  lr:
    min: 1.0e-5
    max: 1.0e-3
  depth:
    min: 2
    max: 6
  batch_size:
    min: 32
    max: 256
  
  optimizer:
    values: ["adam", "sgd"]
